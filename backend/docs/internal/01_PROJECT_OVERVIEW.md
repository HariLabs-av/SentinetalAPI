# Project overview

Student 2 owns the FastAPI backend and deterministic scanner. Student 1 supplies authorized Spring Boot target APIs. Student 3 consumes structured evidence for AI explanations and remediation. Student 4 consumes the versioned REST contract.

The MVP flow is discovery, opaque session creation, credentialed scan, and structured result retrieval. The scanner never performs unrestricted internet scanning.
