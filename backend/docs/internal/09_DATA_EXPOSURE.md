# Data exposure

The first-pass detector flags response keys matching common secret, credential, PII, and internal-metadata names. It reports `probable`/medium confidence, not an automatic definitive judgment. Schema-aware allowlists and domain-specific review remain future work.
