# Scan modes

`easy` is a small demonstration, `moderate` broadens bounded observation, and `brutal` is the highest safe configured bound. All modes remain finite and constrained by the global worker cap.
