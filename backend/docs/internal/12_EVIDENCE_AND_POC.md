# Evidence and PoCs

Each finding has a stable type, status, severity, confidence, endpoint, method, and sanitized evidence. Each PoC identifies the request and authentication context but uses `Bearer <REDACTED>`; a local authorized tester must supply credentials.
